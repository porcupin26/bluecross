#!/bin/sh
set -e

case "$1" in
    purge|remove)
        # Reload udev rules after removing the package
        if command -v udevadm > /dev/null 2>&1; then
            udevadm control --reload-rules || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
